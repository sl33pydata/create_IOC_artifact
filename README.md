# create_IOC_artifact
The final custom function for the automated process of gathering the latest IOCs from alienvault. Again I had a strict environment so had to get creative and do some work arounds. But this takes the parsed list from the parse_otx_pulses and creates them into artifacts within SOAR. This is all so that later can run a seperate playbook for enrichment
